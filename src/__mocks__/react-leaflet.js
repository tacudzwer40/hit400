import React from 'react';

const Mock = ({ children }) => <div>{children}</div>;

export const MapContainer = Mock;
export const TileLayer = Mock;
export const Marker = Mock;
export const Popup = Mock;
