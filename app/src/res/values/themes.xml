<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TittleDeedVerifier" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary_dark</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Camera Theme -->
    <style name="Theme.TittleDeedVerifier.Camera" parent="Theme.TittleDeedVerifier">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Preview Theme -->
    <style name="Theme.TittleDeedVerifier.Preview" parent="Theme.TittleDeedVerifier">
    </style>

    <!-- Result Theme -->
    <style name="Theme.TittleDeedVerifier.Result" parent="Theme.TittleDeedVerifier">
    </style>
</resources>
